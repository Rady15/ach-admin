✅ Remove 'export' and 'add order' buttons from Orders page  
✅ Remove page titles (since navbar shows them)  
✅ Dashboard redesign with RTL layout, stats, charts  
✅ Status section as SVG circle chart with colored segments/proportions  
✅ Legend positioned left of chart (RTL)  

All tasks completed.
